#include <stdio.h>

int main(){
    // This is for quick quiz first problem

        // int i=34;
        // int *j = &i;

        // printf("The value of j before++ is %u\n",j);
        // j++;
        // printf("The value of j after ++ is %u\n",j);

    // This is for quick quiz second problem
        // int f=34;
        // int *g = &f;

        // printf("The value of g before++ is %u\n",g);
        // g++;
        // printf("The value of g after ++ is %u\n",g);


    // This is for quick quiz problem number 3
        // int a=34;
        // int *b = &a;
        // int c =45;
        // int *d= &c;
        // int z = b - d;

        // printf("The value of b is %u\n",b);
        // printf("The value of d is %u\n",d);
        //  printf("The solution for quickl quiz problkem 3 is %u\n",z);

        

return 0;
}
